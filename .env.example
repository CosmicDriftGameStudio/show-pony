# show-pony local dev. Copy to .env (or reuse the parent workspace .env via
# `bun dev`'s --env-file=../.env). Postgres + Redis must be running.
DATABASE_URL=postgres://localhost:5432/show_pony_dev
REDIS_URL=redis://localhost:6379
