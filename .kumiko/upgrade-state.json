{
  "version": "0.274.0",
  "appliedAt": "2026-09-15T05:30:12.745331Z",
  "codemods": [
    {
      "version": "0.267.0",
      "codemod": "scripts/codemod/migrate-db-raw.ts",
      "title": "ctx.db.unsafeRaw(reason) gated by escapeHatch, tenancy: \"global\" invariants, bundled features migrated off ctx.db.raw (fw#2858)."
    },
    {
      "version": "0.269.0",
      "codemod": "scripts/codemod/migrate-open-to-all.ts",
      "title": "`openToAll: true` is removed — every openToAll grant now requires { reason } (fw#2858)."
    },
    {
      "version": "0.271.0",
      "codemod": "scripts/codemod/migrate-db-raw.ts",
      "title": "`TenantDb.raw` is removed — framework infrastructure gets the connection injected (fw#2860)."
    }
  ],
  "pendingManual": [
    {
      "version": "0.274.0",
      "title": "A searchable reference field targeting an encrypted/PII labelField now fails boot unless that label is itself searchable."
    },
    {
      "version": "0.272.0",
      "title": "r.step.read.findOne/read.findMany now tenant-filter by default; cross-tenant reads need unsafeAllTenants + escapeHatch (fw#2914)."
    },
    {
      "version": "0.272.0",
      "title": "ctx.queryAsMember runs the queried handler inside a Postgres READ ONLY transaction — ctx.db and ctx.db.unsafeRaw writes fail (fw#2902)."
    },
    {
      "version": "0.271.0",
      "title": "ctx.queryProjection(..., { unsafeAllTenants: true }) needs r.systemScope() or escapeHatch; EntityTableMeta projection tables are now tenant-filtered (fw#2913)."
    },
    {
      "version": "0.271.0",
      "title": "systemScope handlers default to a per-tenant+handler rate limit; nested-write refuses a foreign-tenant/foreign-owner parent row (fw#2861)."
    },
    {
      "version": "0.271.0",
      "title": "Boot validator requires every screen to be reachable from the app's nav tree."
    },
    {
      "version": "0.269.0",
      "title": "ctx.queryAs/ctx.writeAs to any identity other than the caller itself need r.systemScope() or escapeHatch (fw#2876)."
    },
    {
      "version": "0.266.0",
      "title": "`publicIntake` is removed; an openToAll write handler declares unbound personal data with `openToAll: { reason, personalData: \"tenant-members\" }`."
    },
    {
      "version": "0.266.0",
      "title": "The openToAll personal-data boot check now finds personal-data fields nested anywhere in a write handler's input schema."
    },
    {
      "version": "0.263.0",
      "title": "ctx.queryAs/ctx.writeAs with a SYSTEM identity only from r.systemScope(), jobs, or a handler/hook declaring escapeHatch (fw#2859)."
    },
    {
      "version": "0.263.0",
      "title": "Forms are prefilled from URL query parameters only for fields a declarative `navigate` action declares as `params` (fw#2852)."
    },
    {
      "version": "0.261.0",
      "title": "access is required on handler definitions; openToAll accepts { reason } (+ publicIntake) (fw#2855)."
    },
    {
      "version": "0.259.0",
      "title": "sessions:query:user-session:mine returns the paged envelope { rows, nextCursor }; new self-service my-sessions screen (fw#2844)."
    }
  ]
}
